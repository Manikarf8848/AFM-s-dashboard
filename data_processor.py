def data_cache(func):\n    cache = {}\n    def wrapper(*args):\n        if args in cache:\n            return cache[args]\n        result = func(*args)\n        cache[args] = result\n        return result\n    return wrapper\n\n@data_cache\ndef compute_heavy_operation(data):\n    # Simulate a heavy calculation\n    result = sum(x * x for x in data)\n    return result\n\nclass MemoryManager:\n    def __init__(self):\n        self.used_memory = 0\n\n    def allocate(self, size):\n        self.used_memory += size\n        print(f'Allocated {size}MB, current usage: {self.used_memory}MB')\n\n    def deallocate(self, size):\n        self.used_memory -= size\n        if self.used_memory < 0:\n            self.used_memory = 0\n        print(f'Deallocated {size}MB, current usage: {self.used_memory}MB')\n\n# Example usage\nif __name__ == '__main__':\n    mm = MemoryManager()\n    mm.allocate(10)\n    mm.deallocate(5)\n    mm.allocate(20)\n    print(compute_heavy_operation((1, 2, 3, 4, 5)))